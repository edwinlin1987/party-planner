import React from 'react';
import ReactDOM from 'react-dom';

const initialState = window.__INITIAL_STATE__;

const ROOT_NODE = document.getElementById('root');

ReactDOM.render(<div>TESTING</div>, ROOT_NODE);
